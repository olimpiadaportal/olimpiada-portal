// Skeleton for the forgot-password page (single-field auth form).
import { AuthFormSkeleton } from "@/components/skeletons/pages";

export default function Loading() {
  return <AuthFormSkeleton maxWidth={440} fields={1} />;
}
