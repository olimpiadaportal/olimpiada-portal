// Skeleton for the parent notification center (shared composition).
import { NotificationsSkeleton } from "@/components/skeletons/pages";

export default function Loading() {
  return <NotificationsSkeleton />;
}
