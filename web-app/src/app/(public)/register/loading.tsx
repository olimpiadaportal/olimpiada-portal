// Skeleton for the parent registration page (440px column, 5-field form).
import { AuthFormSkeleton } from "@/components/skeletons/pages";

export default function Loading() {
  return <AuthFormSkeleton maxWidth={440} fields={5} />;
}
