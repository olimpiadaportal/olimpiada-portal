// Skeleton for an in-panel news article (shared NewsArticleView composition).
import { NewsArticleSkeleton } from "@/components/skeletons/pages";

export default function Loading() {
  return <NewsArticleSkeleton />;
}
