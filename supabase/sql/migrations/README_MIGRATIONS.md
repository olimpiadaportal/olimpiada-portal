# Supabase Migrations README

## Repository Placement and Related Files

- Intended path: `supabase/sql/migrations/README_MIGRATIONS.md`
- Folder: `supabase/sql/migrations/`
- Primary readers: Supabase implementer, Claude Code, database reviewer
- Related files: `supabase/sql/README_DATABASE_VERSIONING_WORKFLOW.md`, `supabase/README_RUN_ORDER.md`, `supabase/markdowns/SUPABASE_SQL_RUN_ORDER.md`
- Scope controlled by this file: incremental SQL migration rules
- Source-of-truth level: derived migration execution guide

## Purpose

This folder stores incremental SQL changes after the canonical root SQL files already exist.

Use this folder for:

- hotfixes
- schema changes
- RLS fixes
- new indexes
- function/trigger updates
- seed updates
- data backfills
- production patches

## Naming

```text
YYYY_MM_DD_NNN_short_description.sql
```

Example:

```text
2026_01_18_001_fix_parent_student_rls.sql
```

## Required Header

Every migration must begin with:

```sql
-- Migration: YYYY_MM_DD_NNN_short_description.sql
-- Purpose:
-- Environment first applied:
-- Related root SQL file(s):
-- Backport status: pending | completed
-- Destructive change: no | yes
-- Rollback notes:
```

## Backport Requirement

After a migration is tested and accepted, update the relevant root SQL file in `supabase/sql/` so the complete database can be rebuilt from scratch.

Example:

- Migration file: `migrations/2026_01_18_001_fix_parent_student_rls.sql`
- Backport target: `010_rls_policies.sql`

Update `STATUS.md` after backporting.

## Production Safety

Do not run untested migrations in production.

Required order:

1. development/staging
2. validation queries
3. backup check
4. production migration
5. production validation
6. `STATUS.md` update

## Never Do This

- Do not leave dashboard-only SQL changes undocumented.
- Do not create random SQL files outside `supabase/sql/`.
- Do not change production first.
- Do not skip backporting.
- Do not run destructive SQL without written approval in `STATUS.md`.

## Migration Log

Older migrations document themselves in their own file headers; this log starts
where it was introduced.

| # | Date | Purpose | Backported into |
|---|------|---------|-----------------|
| 115 | 2026-08-15 | "Report a problem": `question_reports` table, the BEFORE INSERT trigger that derives every context column and enforces the 5/hour + 20/day throttle, the freeze trigger that keeps a filed report immutable, `submit_question_report()`, RLS (reporter reads own / admin reads all and moves status, no delete policy). | `001` (enums), `008` (table), `010` (RLS + policies), `011` (indexes, triggers, RPC, grants), `015` (the olympiad-package FK, which cannot live in `008` — `olympiad_packages` does not exist yet at that point in the run order), `013` (check `103`) |
| 116 | 2026-08-16 | Contact addresses become settings (`contact.info_email` beside the existing `contact.support_email`, both seeded with the real olympiq.ai addresses) plus a platform-wide `bug_reports` feature: table, derive/freeze triggers, `submit_bug_report()`, RLS, three enums, and a Brevo transactional-mail path. **Withdrawn one day later by 117** — see that row before reading this one. | `001`, `008`, `010`, `011`, `012` (the two contact settings, which SURVIVE), `013` (check `104`) |
| 117 | 2026-08-17 | Withdraws `bug_reports` (owner decision: ONE reports section, no email anywhere). Drops the table, its policies/indexes/triggers, `submit_bug_report()` and the three enums that were exclusively its own — `report_platform` is KEPT because `question_reports.platform` shares it, and `012`'s two contact settings are untouched. In its place, question-report triage now tells the reporter: an AFTER UPDATE trigger notifies them in the language they filed in when a report is taken into review, resolved or dismissed, idempotent per (report, status). Also fixes two pre-existing defects on `question_reports`: the freeze trigger was reverting its own `on delete set null` cascade into a dangling reporter id, and `qreports_select` let a reporter read `admin_note`/`handled_by` through PostgREST column selection. | `001` (enums), `008` (table), `010` (`qreports_select` now admin-only), `011` (notifier + freeze carve-out), `013` (check `104` rewritten as `104_bug_reports_withdrawn`) |
| 118 | 2026-08-17 | Retires PRORATION and the single shared renewal date per child (owner decision reversing an earlier one): drops `quote_subject_change` and `apply_subject_change`, the two add/remove wrappers that were the last reachable route into that model. Migration `109` had already moved the billing itself — `apply_plan_change` opens each subject's OWN period at `now()` and charges it in full — so nothing about pricing changes here; what changes is that a caller which sends only subject ids can no longer reach a different, prorating RPC. The web app derives the per-subject basket server-side instead (`web-app/src/lib/planBasket.ts`), in one place, and always calls the plan pair. No CASCADE: a dependency must abort the migration loudly. | `007` (ledger comment), `010` (policy comment), `011` (both bodies + their grants removed, `assert_payments_enabled` comment reworded), `013` (checks `78`/`84`/`91`/`95` re-pointed at `apply_plan_change`, new check `105` asserts both wrappers stay GONE) |
| 119 | 2026-08-17 | Makes the question EXPLANATION genuinely trilingual. **No schema change** — `question_explanations` has had per-locale rows, `uq_explanation_locale` and locale-agnostic RLS since `004`; the gap was the write path and the disclosure. (a) BOTH bulk importers nested the `question_explanations` insert INSIDE the `translations->loc->>'body' <> ''` guard, so a row supplying an `en`/`ru` explanation with no body in that locale lost it with **no row and no error** — the guard now wraps the translation row only. The payload contract is unchanged (`translations.<locale>.explanation`), so legacy az-only files and the 2897 live az rows import and behave exactly as before; the fix is purely additive. (b) `get_test_review` resolved reader-locale-then-az correctly but returned one string with no hint which locale produced it — both branches now also emit `explanation_locale` and `explanation_is_fallback`. Additive keys only: `explanation` stays a nullable STRING because the shipped mobile binary casts this payload without validating it and expo-updates cannot cross a runtimeVersion. The server flag is authoritative where the apps' own probe cannot be — SECURITY DEFINER sees an archived question's rows that `qexpl_select` hides, and it reads a legacy daily round's FROZEN `content_snapshot` directly. (c) Adds `ck_qexpl_body_len` (20000) as an abuse ceiling — `NOT VALID`, so legacy rows are never scanned. | `011` (all three function bodies + their revoke/grant lines re-issued verbatim, item-shape comment), `004` (the ceiling, inline+VALID on a fresh build), `013` (new check `106`) |
| 120 | 2026-08-17 | Cancelling a scheduled subject removal becomes an UN-CANCEL, not a purchase. Both plan RPCs used one hand-copied `not exists (… and ss.remove_at is null)` to answer two different questions ("is this on the go-forward plan?" vs "must this be bought?"), so a removal-scheduled subject — still paid for to its own period end — was billed a second full period today and had `current_period_start/end` reset to `now()`, destroying the remaining prepaid time. Replaced by ONE shared classifier `plan_change_states()` returning `covered`/`reinstate`/`add`; a reinstatement clears `remove_at` and nothing else, charges zero, and is logged as `change_type = 'reinstate'` with `prorated_amount = 0`. A LAPSED row is still a true add. Matches the standard un-cancel semantics (Stripe `cancel_at_period_end = false`). | `007` (the change_type CHECK gains 'reinstate'), `011` (classifier + both RPCs), `013` (new check `107`; check `95` re-pointed off the retired `left join public.subscription_subjects ss` anchor) |
| 121 | 2026-08-18 | DELETES the demo payment mode (owner decision). The platform had FOUR modes resolved from three mutually-exclusive feature flags — real (`payments`), demo (`demo_payments`), giveaway (`giveaway_period`), off — and demo was the temporary "cosmetic card form, nothing is charged" stand-in for a payment provider. The flag ROW is deleted; `fn_payment_mode_exclusivity()` now enforces exclusivity over the PAIR and **rejects** any `demo_payments` row on insert (`check_violation`, hint `demo_payments_removed`), with the trigger's WHEN clause routing such a row into the guard REGARDLESS of `enabled` so a disabled row cannot reappear either; both SQL resolvers (`current_payment_mode`, `get_mobile_config`) lose their demo branch. Both are patched from their OWN live definition per the house rule, never retyped. `off` deliberately STAYS — it is the kill switch and the fail-closed fallback, not a payment method — so the surviving modes are real \| giveaway \| off. The migration does NOT enable `giveaway_period` (that stamps `giveaway.started_at` and starts the free-window clock — an owner decision made from /settings), so production legitimately lands in `off`; `giveaway.duration_days` is untouched. | `011` (exclusivity function + trigger, both resolvers, the comment blocks that described a trio), `012` (the `demo_payments` seed row is GONE — a from-zero build that seeded it would now abort on the guard), `013` (check `33` asserts the PAIR, check `57`'s mode whitelist drops `demo`, new check `108`) |
| 122 | 2026-08-19 | The ADMINISTRATOR writes the answer a student receives. Migration `117` gave the reporter a reply loop with FIXED copy — the same sentence for a wrong answer key, a broken image and a misunderstanding — so it answered none of them. Closing a report now carries an admin-written body, stored on the report as the new nullable `question_reports.resolution_message` (CHECK: trimmed, 10..1000; TRIMMED before measuring so ten spaces is not a ten-character answer). New `question_report_reply_text(locale, created_at, body)` is the ONE definition of what is delivered: a generated opening line naming the filing date and time in **Asia/Baku** (`DD.MM.YYYY` / `HH24:MI`), the body, and a generated closing line, joined by BLANK lines, all in the locale the report was filed in (`question_reports.locale`, never the admin's UI). The admin panel's live preview is a pinned TypeScript port of it. Two behaviour reversals, both owner decisions: (a) the send is REQUIRED — a `resolved`/`dismissed` transition with nothing to say RAISES; (b) migration `117`'s `exception when others then raise warning` swallow is REMOVED, so a failed send aborts the transition and the status does not change. A SUPPRESSED notification is not a failure: `create_notification` returning NULL (recipient has in-app notifications off, or the key was already used) commits normally and the reply is still stored. The idempotency key gains an `md5(reply)` discriminator so a CORRECTED answer after a reopen is not deduped against the wrong one. `question_report_freeze` is re-issued for COMMENTS ONLY — `resolution_message` is writable precisely because it is absent from the restore list, and "completing" that list would discard every reply with nothing failing. Deliberately NO table-level `resolved implies a message` CHECK: it would be re-evaluated on the `on delete set null` cascade and make deleting a pre-122 reporter's account fail. | `008` (the column, its CHECK and its comment), `011` (new `question_report_reply_text` + revokes; `notify_question_report_status_tg` rewritten; `question_report_freeze` re-commented), `013` (new check `109`) |
| 123 | 2026-08-19 | Prepares the ledger for the first AzeriCard/ABB transaction on the bank's sandbox terminal. Two changes. (a) NEW partial unique index `uq_checkout_provider_session` on `checkout_sessions (provider, provider_session_id)` — the gateway spec makes the last six digits of a merchant ORDER the system trace audit number and requires them to be unique per terminal per day; we mint `YYYYMMDD` (UTC) + six CSPRNG digits, which leaves only the six digits to chance (~39% odds of at least one collision at a thousand orders a day), and a collision would let the TRTYPE 90 status query for one payment answer about another. "Check then insert" cannot close it — two concurrent requests both see the gap — so the mint loop inserts and retries on SQLSTATE 23505 and the index is what makes that correct. Partial and provider-keyed so providers can never collide with each other's id space. (b) `checkout_sessions.kind` gains `'protocol_test'`: an acquirer integration test is neither a subscription nor an olympiad, and recording it as one would leave a sale nobody can explain in every future reconciliation report. The CHECK is dropped and re-added under its auto-generated name so a from-zero build from `007` and a live database patched here agree. Deliberately NOT in this migration: any entitlement table, grant path, card column or token/recurring column — access must be governed by the provider-agnostic `entitlements` table of `docs/STORE_PAYMENTS_COMPLIANCE.md` §4.1, which is its own piece of work, and the payment layer shipping alongside records money and grants nothing. | `007` (the widened inline `kind` CHECK), `011` (the unique index), `013` (new check `110`) |
| 124 | 2026-08-20 | Creates `public.entitlements` — THE access record required by `docs/STORE_PAYMENTS_COMPLIANCE.md` §4.1 — and moves every access gate onto it. Until now four functions read access straight off the subscription and purchase rows, which is the arrangement that turns "Apple forces IAP" into a rewrite: an Apple grant has no AZN price, no sibling discount and no billing container to hang off a `child_subscriptions` row. GRAIN: one row per GRANT, keyed `(source, external_ref)`, with deliberately NO uniqueness on (student, product) — the same child+subject must be able to hold a live `abb_web` grant AND a live `apple_iap` grant at once. There is no stored `status`: liveness is computed (`revoked_at is null and starts_at <= now() and (ends_at is null or ends_at > now())`), because this codebase already ran the other experiment and `recompute_child_access()` exists solely to repair `students.access_status`. The producers stay authoritative for MONEY: three column-scoped triggers mirror them through ONE mapping expression (`fn_entitlement_map_subject` / `fn_entitlement_map_purchase`), and `entitlements_reconcile()` re-runs that same expression hourly at :22 — so trigger, reconciler and backfill are provably the same computation and check `114` proves the table matches the producers rather than an expression matching itself. Behaviour is preserved byte-for-byte: the mapper's end date is the old gate's own conjunction (`least(cs.current_period_end, ss.current_period_end)`), and `remove_at` is deliberately NOT consulted because `admin_manage_child_subscription('extend')` pushes a subject's period end forward without touching it. The giveaway window and free-access intervals stay LAZY inside `has_subject_access()` rather than being materialised into per-child rows. `can_view_olympiad_package` is EXTENDED, never refactored — its purchase branch is kept verbatim because `olympiad_purchases.student_profile_id` is ON DELETE SET NULL (audit M13) while `entitlements.student_profile_id` is NOT NULL and CASCADEs, so lifetime visibility for an ARCHIVED package survives a deleted child. The AzeriCard callback is deliberately NOT given a write path: the `sub:`/`oly:` ref namespace is refused by `entitlement_grant()`, so the ABB rail must write the PRODUCER row and let the trigger mirror it. Payment mode is untouched (`off`). | `001` (two enums), `007` (the table + its seven named CHECKs), `010` (RLS array + the SELECT-only policy — no write policy for anyone, admins included), `011` (seven indexes, updated_at + audit triggers, table privileges, the mapper/reconciler/grant surface, the two subscription triggers, `has_subject_access`, `live_package_entitlement`, `my_accessible_subjects`, the four rewritten gates, `subject_deletion_blocks`), `013` (new checks `111`–`116` AND amendments to `37`/`42`/`46`, which asserted substrings this change moves), `015` (the two deferred FKs, the purchase trigger, the extended `can_view_olympiad_package`, `olympiad_package_deletion_blocks`), `016` (the reconciler cron at :22) |
| 125 | 2026-08-20 | Inverts the parent checkout so the PAYMENT causes the GRANT. Until now `subscribeChild` / `updateSubscriptionSubjectsAction` applied the plan through `create_child_plan` / `apply_plan_change` and only THEN opened a charge, through a helper that "can only return null — it never fails the change"; `subscription_subjects.current_period_end` was written unconditionally, migration `124`'s mirror turned that into a live entitlement immediately, and nothing expires an unpaid subscription — so a parent could add a 90 AZN yearly subject, confirm, close the tab before paying, and keep a live year of access with no `payments` row, repeatably. It could not simply be reordered because `checkout_sessions` recorded an AMOUNT and never a PURCHASE: no student, no subject list, no cycles, so a verified payment had nothing to act on. This adds the INTENT — eight columns on `checkout_sessions` (`intent_kind`, `student_profile_id`, `intent_items`, `intent_quote`, `expires_at`, `redeemed_at`, `redemption_status`, `redemption_note`) rather than a `checkout_session_items` child table, because the intent and the price it was quoted at must be ONE indivisible row, because the jsonb payload IS `plan_items_normalize`'s existing input contract, because freezing a column set is one trigger where freezing a child collection is three, and above all because an FK from items to `subjects` would either CASCADE (silently shrinking a signed basket, delivering a plan nobody authorised) or RESTRICT (a stale pending checkout blocking a subject deletion) — with jsonb the basket cannot move under the payer and redeem fails loudly instead. `checkout_intent_open` quotes and inserts in ONE transaction, so the stored amount is provably the RPC's own number; `checkout_redeem_plan` is the ONLY path from a verified payment to an applied plan: it requires `status = 'paid'`, locks the row, RE-PRICES the frozen basket and demands EXACT equality with the amount the gateway confirmed — a price change, a withdrawn subject, a cancelled subscription or a payments-off flip is recorded as `needs_review` with a reason and left for a human, never silently delivered as a different plan and never silently dropped. Exactly-once is `redeemed_at` set inside the apply's own transaction (both outcomes are terminal), over the existing `payments` / `payment_events` unique keys and `apply_plan_change`'s replay guard, keyed here on the ORDER rather than the interactive 5-minute bucket. Abandonment is now harmless BY CONSTRUCTION. `quote_child_plan` is re-issued to apply `create_child_plan`'s own one-trial-per-child rule and return `due_now`, so the start-a-plan screen's "due today" is the number the gateway is asked for (audit H7); it previously contradicted the charge in both directions. Nothing here writes `entitlements` — redemption goes through the producers and lets `124`'s triggers mirror. Payment mode untouched (`off`). | `001` (two enums), `007` (the eight intent columns inline + `ck_checkout_intent_shape` / `ck_checkout_redemption` + column comments), `011` (three partial indexes, `fn_checkout_intent_immutable` + its trigger, the re-issued `quote_child_plan`, and the four intent functions with their revoke/grant lines), `013` (new checks `117` and `118`, and an amendment to `114` — its `new_*` sides read EVERY live entitlement, so the first legitimate manual/apple_iap/school_license grant turned it FAIL permanently and masked real drift; both sides are now scoped to producer-linked rows) |
| 126 | 2026-08-20 | Closes the three routes to a PAID PLAN WITHOUT A PAYMENT that `125` did not reach, and gives a lost callback a way home. **(A) The purchase-silent surface could buy.** The mobile BFF called `create_child_plan` / `apply_plan_change` directly with no checkout anywhere, so a parent bearer token reached a full paid plan for free the moment the payment mode became `real` — a store-policy failure as much as a money one, since `docs/STORE_PAYMENTS_COMPLIANCE.md` §4 makes the apps purchase-silent BY ARCHITECTURE and Google's consumption-only test is APP-WIDE. New `create_child_plan_if_free` / `apply_plan_change_if_free` apply the change and then RAISE `check_violation`/`payment_required` if the apply's OWN return value priced it above zero, rolling back the apply, its ledger rows and the entitlement rows `124`'s producer triggers wrote. The verdict is taken inside the SAME statement precisely because a pre-check cannot be: prices, the sibling tier and `launch_promo_config.trial_days` can all move between a quote and an apply under READ COMMITTED. Separate NAMES rather than a boolean parameter, so the priced behaviour cannot be reached without writing the priced function's name — and so the old signature does not survive as a bypass. Removals, reinstatements (`120`), scheduled cycle changes, giveaway windows, admin free-access intervals and trials all price at zero and still work from the app. **(B) A running trial made every addition free forever.** `quote_plan_change` prices a trialing add at ZERO and documents it as 'riding the trial'; `apply_plan_change` anchored every add at `now()` + its FULL cycle regardless of status, so a yearly subject added on day one of a seven-day trial bought a YEAR for nothing — repeatable, with no obligation recorded and nothing that could ever collect it (there is no renewal path, and card-on-file is not approved by the bank: AZCDF-100303). A trial-time add now ends at the TRIAL END, exactly as `create_child_plan` already writes the opening basket, so the rule is uniform: WHILE TRIALING, EVERY SUBJECT PERIOD ENDS AT THE TRIAL END. The trial stays a bounded, self-terminating free window and can no longer become a free PAID period; `quote_plan_change`'s renewal date is moved to match, because H7 covers the dates and not only the amounts. Other half of the same finding: with `launch_promo_config.trial_days = 0` `create_child_plan` still took the `trialing` branch and wrote `trial_ends_at = now()` — a period that had ALREADY ENDED — while the quote read the same 0 and charged the FULL total. A plan is now `trialing` only when it has trial days left to run. **(D) A lost callback had no way home.** A payment authorised at the bank whose BACKREF POST never arrives leaves the family charged with no record, no plan and no alarm, and the gateway answers `TRTYPE=90` for only 24 HOURS. `checkout_reconcile_candidates()` names the pending intents worth asking about (inside the window, at least five minutes old) and `checkout_redeem_sweep()` redeems sessions the ledger already records as `paid` whose redemption never ran — both through `checkout_redeem_plan`, never a second copy of that logic. The split is forced by a secret, not a preference: the status query needs a MAC signed with the merchant private key, which lives only in the web app's environment and must never enter the database, and there is no `pg_net` here — so SQL names the orders, the web-app sweep asks, and the pg_cron job is the no-network floor under it. The sweep deliberately skips a `plan_start` whose child has no 8-digit ID yet, because finishing one needs a Supabase Auth admin call SQL cannot make. Payment mode untouched (`off`); no flag enabled, no card or token column, no new grant to `anon`/`authenticated`, nothing given a direct write path into `entitlements`. | `011` (the re-issued `create_child_plan` / `quote_plan_change` / `apply_plan_change`, the two `*_if_free` wrappers and the two reconciliation functions, each with its revoke/grant pair), `016` (the `olympiq_checkout_redeem_sweep` job, inside the existing pg_cron guard), `013` (new checks `119` and `120`) |
| 127 | 2026-08-21 | Closes the LAST route to access without money, honours the price we quoted, and ends the two silences. **(1) The WEB olympiad purchase granted LIFETIME access for free.** `olympiadCore.processOlympiadPayment` was a documented MOCK returning `{ ok: true }` unconditionally and never wired to anything: `purchase_olympiad` wrote an ACTIVE purchase, `124` mirrored it into a lifetime entitlement, and no `payments` row existed anywhere. `126` closed only the mobile half, so this was the single remaining reason the payment mode could not become `real`. Fixed on the SAME rail `125` built rather than a second one — `checkout_intent_kind` gains `'olympiad'`, `checkout_intent_open` prices it through the new `quote_olympiad_purchase` and freezes `[{package_id, grade_id}]`, `checkout_redeem_plan` gains a branch — so exactly ONE function in this database still turns money into access. The GRADE is frozen because `purchase_olympiad` snapshots it and attempts draw that pool forever, so a promoted child is `grade_changed` rather than a silently different purchase; redemption also stamps `olympiad_purchases.provider = 'azericard'` — and `trg_entitlements_from_purchases` gains `provider` on its COLUMN LIST, without which the stamp changed the row and re-fired nothing, leaving every paid package filed as a COMPED (`manual`) grant and paid revenue invisible to any report keyed on `source`. The same statement records the amount the parent was actually CHARGED, because `purchase_olympiad` writes today's catalog price and an honoured frozen price would otherwise leave the purchase row and the `payments` row disagreeing about the same money. The duplicate `buyOlympiad` action, which called `purchase_olympiad` directly with no quote and no payment, is deleted. `ck_entitlement_lifetime` and `can_view_olympiad_package` are untouched: a purchased package still never expires and stays visible after archival. **(2) The frozen price is HONOURED (owner decision).** Exact equality at redemption fired on ordinary behaviour — paying for child A moves child B's sibling tier, so B's signed intent re-priced differently and B's money landed in `needs_review` over a few AZN. Redemption now delivers at the quoted amount and records `honoured_frozen_price` with both numbers in `payment_events`. The line is drawn at DELIVERY, not price, and it is drawn with a COMPARISON rather than with a number: redemption re-derives the change from the projection using the same `plan_change_delta` that froze it and requires the two to be IDENTICAL — same subjects, same nature, same cycle — before honouring anything. Read as "the amounts differ, so the price moved" the rule is wrong in both directions: a delivery that SHRANK (a second tab delivered part of the basket first) re-prices lower and would be charged the larger frozen amount, and one that GREW (a frozen FREE reinstatement whose coverage lapsed is now a paid add) re-prices higher and would be handed over at the smaller. `checkout_intent_price` asks the same question BEFORE money moves — `delivery_changed` beside `price_changed`, which is what bounds the exposure to the signing-to-redeeming window — and a withdrawn subject, a deactivated pricing row, an off-sale package, a promoted child, a vanished subscription or a re-price that comes back at ZERO (`no_longer_payable` — keeping money for something now free) still reach a human. It also stops comparing the frozen grade with `students.grade_id`: a legacy GRADE-LESS package quotes `grade_id = NULL`, so that comparison reported `grade_changed` for every one of them, no checkout could be resumed, and the duplicate-purchase guard on the resume path went with it. The sibling discount is also made VISIBLE where the parent chooses: `quote_plan_change` returns `rank`, and the summary names the tier, prints the saving and — with no discount yet — says a second child is cheaper. **(3) The web free branch used the unguarded RPC.** `subscribeChild` / `updateSubscriptionSubjectsAction` quoted, saw `dueNow == 0` and called the priced apply — the exact quote-then-apply race `126` declared indefensible from an app server, and indefensible from the web for the same reason. Both now use the `_if_free` wrappers, and a new `purchase_olympiad_if_free` gives the package path the same shape; after this migration NO application code path names a priced apply RPC at all. **(4) Two trial edges.** `status = 'trialing'` was read as "a trial is running" although the status is swept by a job rather than by the clock, so a lapsed trial priced every addition at ZERO for as long as the row stayed stale; and a trial-bounded add was capped at `trial_ends_at` with no check it was still in the future, so it could be applied free with an already-expired end. Both become one predicate computed identically in the quote and the apply: a trial runs only while `trial_ends_at` is in the FUTURE. **(5) A stale frozen basket could UN-CANCEL a subject** — and, unreported, could REMOVE one added after the intent. An absolute basket is a claim about the whole plan at a past moment. A `plan_change` intent now freezes the DELTA (`plan_change_delta` → the new frozen `checkout_sessions.intent_delta`) and redemption PROJECTS it onto coverage as it is NOW (`plan_delta_project`); `intent_items` is kept as the evidence of what the parent saw. A pre-127 session carries no delta, cannot show that what would be delivered is what was authorised, and is therefore recorded for a human rather than delivered on a guess. What a redemption ACTUALLY applied is written to the new `checkout_sessions.delivered_items` in the same statement that stamps `redeemed_at`, and it is what a reversal revokes from. **(6) `needs_review` reached nobody** but 013 check 118 — a file somebody runs when they already suspect something. `checkout_alert_admins()` now files a PRIORITY 1 notification (the level `create_notification` refuses to let a recipient silence) from redemption, from `checkout_flag_redemption` and from a reversal, and cannot raise. A new Administrator-only page `/subscriptions/checkouts` shows the queue and GRANTS NOTHING. The alarm gets an off switch that does not rewrite history: `admin_resolve_checkout_review` writes `resolved:<sentence>` into the NOTE plus an audit row and leaves the status alone, because there are two terminal statuses and neither means "a person settled this" — moving one to `applied` would be a lie about a refunded case. **(7) A gateway REVERSAL was invisible.** The live test on 2026-08-21 (RRN 623279219080, TRTYPE=22) surfaced two undocumented facts: the gateway acknowledges a reversal with the single character `1`, and a `TRAN_TRTYPE=1` status query reports the ORIGINAL authorisation as approved FOREVER — the reversal appears only under `TRAN_TRTYPE=22`, which `queryTransactionStatus` hardcoded away. `queryReversalStatus()` plus a third reconciliation pass over `checkout_reversal_candidates()` act only on a fully reconciled answer; `interpretReversalResponse` maps `1` to accepted and EVERYTHING else — a decline-looking body included — to `unknown`, never to declined, because concluding a reversal failed from an undocumented body would keep access alive for returned money. The sweep's own verdict is three-valued (`classifyReversalAnswer`): `reversed` only on a fully reconciled APPROVAL to the `TRAN_TRTYPE=22` question, `not_reversed` on the definitive `rc = -24` / "Transaction context mismatch" the terminal answers for an order with no reversal, and `unreadable` for everything else — which changes nothing and flags the session for a person, because revoking a paying family's access on a maybe is worse than a refund recorded late. `checkout_revoke_reversed()` marks the payment refunded and revokes ON THE PRODUCER (purchase → `refunded`; the subjects named by `delivered_items` — what the redemption applied, never the frozen intent, which after any move names a subject a DIFFERENT payment paid for — have their period closed at `now()`), and cancels the subscription only when NO coverage is left on it, so a plan_start reversal can no longer kill subjects a later, un-reversed payment bought; `124`'s mirror does the entitlement. A redemption decided before `delivered_items` existed revokes nothing and asks for a person. The 24-hour status window is a real limit and is stated rather than worked around. **(8) A checkout whose child was deleted mid-flight showed SUCCESS.** The web callback folded `student_profile_id` — the column the child-delete FK NULLs — into "is this a family checkout", so such a session was treated as the owner's protocol test: redemption was skipped entirely and the parent was shown a completed-payment page for money that delivered nothing and was never recorded as undelivered. The test is now the INTENT alone, and `checkout_redeem_plan` answers the child question itself (`student_gone` → `needs_review` → a priority-1 alert). Payment mode untouched (`off`); no flag enabled, no card or token column, no new grant to `anon`/`authenticated`, nothing given a direct write path into `entitlements`. | `001` (the `'olympiad'` enum value — and note that NOTHING in the migration may evaluate it, since PostgreSQL forbids using a new enum value in the transaction that adds it), `007` (`intent_delta`, `delivered_items` + the re-issued `ck_checkout_intent_shape` + column comments), `011` (nine functions re-issued and eight added, each with its revoke/grant pair carried explicitly), `013` (new checks `121`, `122`, `123`, and an amendment to `118` so a redemption an operator has RESOLVED stops counting as waiting on a human), `015` (`trg_entitlements_from_purchases` re-created with `provider` on its column list) |
