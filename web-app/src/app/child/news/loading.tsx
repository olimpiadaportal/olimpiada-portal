// Skeleton for the student news list (arena heading + shared NewsBrowser grid).
import { NewsListSkeleton } from "@/components/skeletons/pages";

export default function Loading() {
  return (
    <section>
      <NewsListSkeleton eyebrow />
    </section>
  );
}
