# Olimpiada Portal — Web App (Student/Parent) environment template.
# Copy this file to `.env.local` and fill with your Supabase DEV/STAGING values.
# Get these from: Supabase Dashboard -> Project Settings -> API.
#
# NEXT_PUBLIC_* values are sent to the browser. ONLY the project URL and the
# anon (public) key are safe to expose here. NEVER put the service role key in
# this app — it is a student/parent client.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
