// Elmly Design System — Barrel Export
// Import everything from here: import { motion, brandColors, spacing, ... } from '../design-system';

export { brandColors, gamificationColors } from './colors';
export { gradients as colorGradientPairs } from './colors';
export { motion, duration, spring, easing } from './motion';
export { fontSizes, fontWeights, lineHeights, typography } from './typography';
export { spacing, borderRadius } from './spacing';
export { lightShadows, darkShadows } from './shadows';
export type { ShadowPreset } from './shadows';
export { gradients } from './gradients';

// Recommended mobile UI primitives for new feature work live in
// `src/components/ui`. Keep design tokens here and components there.
